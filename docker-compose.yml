services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - path: .env
        required: false
    environment:
      DATA_DIR: /tmp/vedaai
    tmpfs:
      - /tmp/vedaai:size=512m

